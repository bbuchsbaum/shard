# Kill a Worker Process

Terminates the worker process and closes connections.

## Usage

``` r
worker_kill(worker)
```

## Arguments

- worker:

  A `shard_worker` object.

## Value

NULL (invisibly).
