# Check if running on Windows

Check if running on Windows

## Usage

``` r
is_windows()
```

## Value

A logical scalar: `TRUE` if running on Windows, `FALSE` otherwise.

## Examples

``` r
is_windows()
#> [1] FALSE
```
