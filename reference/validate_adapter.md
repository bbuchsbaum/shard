# Validate Adapter Structure

Internal function to validate an adapter has the required structure.

## Usage

``` r
validate_adapter(adapter, class_name)
```

## Arguments

- adapter:

  A list with adapter functions.

- class_name:

  The class name being registered.

## Value

TRUE if valid, throws error otherwise.
