# Check if Worker is Alive

Tests whether the worker process is still running.

## Usage

``` r
worker_is_alive(worker)
```

## Arguments

- worker:

  A `shard_worker` object.

## Value

Logical. TRUE if worker is alive.
