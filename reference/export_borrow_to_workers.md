# Export Borrowed Inputs to Workers

Exports borrowed data to all workers (once, reused across shards).

## Usage

``` r
export_borrow_to_workers(pool, borrow)
```

## Arguments

- pool:

  Worker pool.

- borrow:

  List of borrowed inputs.
