# Format Bytes as Human-Readable String

Format Bytes as Human-Readable String

## Usage

``` r
format_bytes(bytes, digits = 1)
```

## Arguments

- bytes:

  Numeric value in bytes.

- digits:

  Integer. Decimal places (default 1).

## Value

Character string like "1.5 GB".
