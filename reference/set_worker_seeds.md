# Set Worker Seeds

Sets reproducible RNG seeds in workers.

## Usage

``` r
set_worker_seeds(pool, seed, num_shards)
```

## Arguments

- pool:

  Worker pool.

- seed:

  Base seed.

- num_shards:

  Number of shards for substream calculation.
