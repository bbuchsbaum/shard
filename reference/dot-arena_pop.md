# Pop an arena from the stack

Pop an arena from the stack

## Usage

``` r
.arena_pop(arena_id, run_gc = FALSE)
```

## Arguments

- arena_id:

  The ID to verify we're popping the right arena

- run_gc:

  Whether to trigger GC after pop
