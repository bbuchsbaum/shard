% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/doShard.R
\name{registerDoShard}
\alias{registerDoShard}
\title{Register the doShard Backend}
\usage{
registerDoShard(
  workers = NULL,
  mem_cap = "2GB",
  recycle = TRUE,
  profile = c("default", "memory", "speed"),
  packages = NULL,
  init_expr = NULL,
  chunk_size = 1L,
  verbose = FALSE
)
}
\arguments{
\item{workers}{Integer. Number of worker processes. If NULL, uses
\code{parallel::detectCores() - 1}.}

\item{mem_cap}{Memory limit per worker (e.g., "2GB"). Workers exceeding
this limit are recycled.}

\item{recycle}{Logical or numeric. If TRUE, recycle workers on RSS drift.
If numeric, specifies the drift threshold (default 0.5 = 50\% growth).}

\item{profile}{Execution profile: "default", "memory" (aggressive recycling),
or "speed" (minimal overhead).}

\item{packages}{Character vector. Packages to load in workers.}

\item{init_expr}{Expression to evaluate in each worker on startup.}

\item{chunk_size}{Integer. Iterations to batch per worker dispatch.}

\item{verbose}{Logical. Print diagnostic messages.}
}
\value{
Invisibly returns NULL.
}
\description{
Registers doShard as the foreach parallel backend. This function must be
called before using \verb{\%dopar\%} with shard.
}
\examples{
\dontrun{
library(foreach)
library(doShard)

# Register with 4 workers
registerDoShard(workers = 4)

# Use foreach with shard backend
result <- foreach(i = 1:100, .combine = c) \%dopar\% {
  sqrt(i)
}

# Clean up
stopDoShard()
}
}
